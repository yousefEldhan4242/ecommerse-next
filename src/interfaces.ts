export default interface Product {
  id: number;
  name: string;
  main_image: string;
  currentPrice: number;
  quantity: number;
  title: string;
  rating: number;
  discount: boolean;
  availableItems: string;
  previousPrice: string;
  color: string;
}
